# Core pipeline dependencies.
# The canonical data (data/songs/*.json) needs nothing to read — it's plain JSON.
# These are only for regenerating derived formats and audio.

jsonschema>=4.0      # schema validation (tools/validate.py, tests)
mido>=1.3            # MIDI derivation (tools/to_midi.py)
music21>=9.1         # MusicXML derivation (tools/to_musicxml.py)
numpy>=1.24          # notation-to-audio synthesis (tools/synth.py)

# tools/synth.py also shells out to `ffmpeg` for MP3 encoding (optional —
# remove the ffmpeg call to keep WAV output).
